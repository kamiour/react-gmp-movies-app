import { QueryParams } from '../models/QueryParams';

export const initialQueryParams: QueryParams = {
  sortBy: 'release_date',
  sortOrder: 'desc',
  search: '',
  searchBy: 'title',
  filter: '',
  limit: 21,
};
